{
  "name": "my-playwright-monorepo",
  "version": "1.0.0",
  "description": "Playwright monorepo for multiple showcases",
  "private": true,
  "workspaces": [
    "showcase-*"
  ],
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:all": "playwright test --project=chromium",
    "test:smoke": "playwright test --grep @smoke --project=chromium",
    "test:visual": "playwright test --grep @visual --project=chromium",
    "test:deep": "playwright test --grep @deep --project=chromium",
    "report": "playwright show-report --host 127.0.0.1 --port 9333"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0"
  }
}
